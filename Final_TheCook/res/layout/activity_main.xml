<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity" >

    <android.support.v4.view.ViewPager
        android:id="@+id/viewpager_showRandomDishes"
        android:layout_width="match_parent"
        android:layout_height="180dp"
        android:layout_gravity="top" >
    </android.support.v4.view.ViewPager>

    <ListView
        android:id="@+id/listview_showStartMenu"
        android:layout_width="match_parent"
        android:layout_height="150dp"
        android:layout_above="@+id/lin"
        android:layout_below="@+id/viewpager_showRandomDishes" >
    </ListView>

    <LinearLayout
        android:id="@+id/lin"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:layout_alignParentBottom="true"
        android:layout_alignParentLeft="true" >

        <Button
            android:id="@+id/button_haveMateial"
            android:layout_width="80dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:text="我有食材"
            android:textSize="20sp" />

        <Button
            android:id="@+id/button_toCook"
             android:layout_width="80dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:text="我要做菜"
            android:textSize="20sp" />

        <Button
            android:id="@+id/button_myCollect"
             android:layout_width="80dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:text="我的收藏"
            android:textSize="20sp" />
    </LinearLayout>

</RelativeLayout>